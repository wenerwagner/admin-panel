export function LoginPage() {
  return (
    <main className="app-shell auth-shell">
      <section className="placeholder-panel">
        <p className="eyebrow">Admin access</p>
        <h1>Login</h1>
        <p>Authentication will be connected in a later issue.</p>
      </section>
    </main>
  );
}
